import { FormControl } from '@angular/forms';

interface data {}
export interface LoginForm {
  email: any[];
  username: any[];
  password: any[];
}
export interface signUpData {
  email?: string;
  username?: string;
  password?: string;
}
